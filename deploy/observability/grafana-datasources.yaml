apiVersion: 1

deleteDatasources:
  - name: ReleaseProof Prometheus
    orgId: 1

datasources:
  - name: ReleaseProof Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
