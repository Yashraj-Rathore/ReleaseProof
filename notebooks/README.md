# Notebooks
Exploratory notebooks are allowed only for data/ML milestones. Formal logic must migrate into tested packages. Every published experiment must record dataset/split/config/code/artifact lineage; notebook state is not the source of truth.
